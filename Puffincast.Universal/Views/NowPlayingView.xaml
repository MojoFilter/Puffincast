<UserControl
    x:Class="Puffincast.Universal.Views.NowPlayingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Puffincast.Universal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark">
    <UserControl.Resources>
        <Style x:Key="track" TargetType="TextBlock">
            <Setter Property="Margin" Value="5, 5, 0, 20" />
            <Setter Property="Foreground" Value="Gold" />
            <Setter Property="FontStyle" Value="Italic" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock >Now playing</TextBlock>
        <TextBlock Style="{StaticResource track}" Text="{Binding NowPlaying}"></TextBlock>
        <TextBlock>You just heard</TextBlock>
        <TextBlock Style="{StaticResource track}"  Text="{Binding LastPlayed}" />
        
        <TextBlock>Coming up next</TextBlock>
        <ItemsControl ItemsSource="{Binding Next}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Style="{StaticResource track}" Text="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
       
    </StackPanel>
</UserControl>
