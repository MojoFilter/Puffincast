<UserControl
    x:Class="Puffincast.Universal.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Puffincast.Universal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    RequestedTheme="Dark"
    mc:Ignorable="d">

    <RelativePanel>
        <TextBlock x:Name="heading"
                   Style="{StaticResource AppHeading}"
                   RelativePanel.AlignHorizontalCenterWithPanel="True"
                   RelativePanel.AlignTopWithPanel="True" />

        <Pivot RelativePanel.Below="heading"
               ItemsSource="{Binding Areas}"
               ItemTemplateSelector="{StaticResource DefaultTemplates}">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
            
        </Pivot>
    </RelativePanel>
</UserControl>
